<h2 class="dash-title">Edit <%= resource_name.to_s.humanize %></h2>

  <%= render "form" %>

<h3 class="dash-title">Cancel my account</h3>

<p>Unhappy? <%= link_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

<%= link_to "Back", :back %>
